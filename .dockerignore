venv/
.venv/
__pycache__/
**/__pycache__/
.git/
.mypy_cache/
.aider*
*.log
*.out
.env

# Runtime chat history must not be baked into the image - mounted as a
# volume instead (see Dockerfile/DEPLOY.md).
data/chats/

# The pdfs/ working folder isn't needed at runtime: KB creation copies a
# course's source PDFs into data/knowledge_bases/<id>/sources/, which ship
# with the KB (the student app links each citation to its source page).
pdfs/
